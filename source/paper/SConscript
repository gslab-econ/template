Import('env')

env.BuildLyx(target = ['#output/paper/paper.pdf'],
             source = ['#source/paper/paper.lyx',
                       '#output/analysis/plot.eps',
                       '#output/tables/table.lyx'])

env.BuildLyx(target = ['#output/paper/online_appendix.pdf'], 
             source = ['#source/paper/online_appendix.lyx'])

env.BuildLyx(target = ['#output/paper/text.pdf'],
             source = ['#source/paper/text.lyx'])

env.BuildLyx(target = ['#output/paper/ondeck.pdf'], 
             source = ['#source/paper/ondeck.lyx'])