import os
Import('*')

env.PDF(target = '#output/paper/paper.pdf', source = 'paper.lyx')
env.PDF(target = '#output/paper/paper_online.pdf', source = 'paper_online.lyx')
env.PDF(target = '#output/paper/claims.pdf', source='claims.lyx')

