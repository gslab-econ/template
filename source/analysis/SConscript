Import('*')

env.Command(
	target = ['#output/analysis/plot.eps', '#output/analysis/scons.log'], 
	source = ['#output/data/data.txt', 'analysis.do'],
	action = 'statamp -e source/analysis/analysis.do && mv analysis.log output/analysis/scons.log'
	)

	
