Import('env')

env.BuildStata(target  = ['#%s/data.txt'    % env['PATHS']['build']['prepare_data']],
               source  = ['#%s/simulate.do' % env['PATHS']['source']['prepare_data']],
               log_ext =  'simulate')