Import('env')

build  = env['PATHS']['global']['build']['talk']
source = env['PATHS']['global']['source']['talk']

env.BuildLyx(target = '#%s/slides.pdf'  % build, 
             source = ['#%s/slides.lyx' % source])

env.Install('#release', '#%s/' % build)
